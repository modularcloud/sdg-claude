# Specialist mission — DRAFT IMPLEMENTATION.md (Phase 5)

Runs only when `specs/IMPLEMENTATION.md` does not exist. Draft it to PROCESS.md's IMPLEMENTATION.md requirements, covering the technical choices for both programs — product and test harness. Ground the proposal in `specs/SPEC.md` and `specs/GOALS.md`, and confirm it matches Developer preferences via QUESTION before committing.

Commit, push. Report next: the Phase 5 TEST-SPEC draft if `specs/TEST-SPEC.md` is also missing, otherwise Phase 6.
