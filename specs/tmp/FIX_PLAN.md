# FIX_PLAN — Phase 9: implement the test harness

Goal: harness adherence to `specs/TEST-SPEC.md` + `specs/CERTIFICATIONS.md`. All harness self-tests (TEST-SPEC §17 S-1…S-7) and all certifications (C-1 over every CERTIFICATIONS.md fixture) must pass locally (`npm run test:self`) and in the `harness-self` CI job. Product-facing tests are expected to FAIL at this phase — the product is a stub. Product code (`src/`) must not be touched.

Planned 2026-07-10 from the Phase 9 iteration-1 compliance panel (TEST-SPEC.md and CERTIFICATIONS.md scopes) and the red verify run at HEAD 89a52bb. Baseline: zero of the ~260 specified tests implemented; `test/helpers/` and `test/fixtures/` empty; only three self-labeled scaffold smoke tests exist.

## Conventions binding every task (read before starting any task)

- **Scope guard.** Phase 9: edit only harness code (`test/`), `AGENTS.md` (build/lint/run knowledge only), and this file. Never modify `src/`, `specs/` documents, or `.claude/`. The harness never imports product code; it drives the built `xspec` executable (`dist/cli/bin.js`, see `AGENTS.md`) strictly as a subprocess (TEST-SPEC H-2; IMPLEMENTATION.md).
- **Layout** (per `test/vitest.config.ts` comments): shared machinery in `test/helpers/`; product-facing tests (§§1–16) in `test/suite/`; self-tests and the certification runner (§17) in `test/self/`; fixture products and consumer fixture projects in `test/fixtures/`; the E-6 subset in `test/windows/`; `test/local/` stays empty (E-2).
- **One code path (C-2).** Every product-facing test body must be written against a *product binding* (executable path + workspace dir + env), defaulting to the built product, so the certification runner can execute the identical test — addressable by its TEST-SPEC ID, individually and as named subsets — against a fixture executable. No test hard-codes the product path. Keep a registry keyed by test ID; the Vitest files in `test/suite/` are thin wrappers over it.
- **Red-green discipline (H-8).** Against the current stub product (exits 86, no output) every product-facing test must fail with a *diagnosed assertion failure* — never a harness crash, hang, or pass. Definition of done for a `SUITE-*`/`PROP-*` task: tests implemented per their TEST-SPEC text; `npm run typecheck` and `npm run format:check` green; each new test observed failing-as-diagnosed against the stub (run the suite project filtered to the new files); no `.skip` anywhere (H-9).
- **Assertions.** Byte equality wherever SPEC.md fixes bytes; opaque content asserted only by its stated observable contract (H-4). Every test asserts the exact exit code and, where relevant, stdout/stderr separation (H-5). Determinism tests use the run-twice / two-directory protocol (H-6). No wall clock or randomness in assertions (H-10).
- **Violator count.** CERTIFICATIONS.md defines 4 conformers and **13** violators (6 CORE, 2 VALID, 2 MD, 3 DISC). The iteration-1 findings' "12" is superseded by the document itself.
- **Staging constraints** (CERTIFICATIONS.md preamble ¶3) bind the harness's fixtures for named tests and are restated inside the tasks below for T13.5-2, T13.5-3, T13.4-5, T3-1…T3-6, T7-4, T7-5, T7-6. Do not stage those tests differently.
- If a task teaches you anything new about building, linting, or running the code (e.g. a fixture build step), record it in `AGENTS.md`.

---

## A. Harness machinery (`test/helpers/` + `test/self/`)

- [ ] **HARNESS-03 — Byte/determinism assertion helpers.** Implement the shared assertion protocol helpers: byte-equality assertions over outputs and files; whole-workspace and whole-directory byte snapshots and compares (for modifies-nothing and compare-around-command tests); the H-6 determinism protocol (same command twice; identical workspace rebuilt in two directories; assert byte-identical outputs and written files, normalizing nothing); exit-code/stream convention assertions of 12.0 (`--json` stdout is exactly one JSON document or empty on exit-2). Cites: §0 H-4/H-5/H-6. Verify: unit-level self-checks in `test/self/` exercise each helper (positive and negative).
- [ ] **HARNESS-04 — Output adapter layer + S-5 self-test.** Implement the H-3 decoding adapters: per-command thin adapters mapping actual product output (human and `--json`) onto the asserted information model (nodes, hashes, edges, categories, counts, paths, findings, items, …); adjustable to shape, never to values; fail loudly (test error, not pass) when required information is absent. This layer also owns: the shape-aware, value-blind session-corruption staging transformations for T10.1-4 (duplicate an item entry, unknown status, `blockedBy` cycle/absent-id, delete a field, garble recorded parameters — always starting from a product-written file, failing loudly on shape mismatch); and the sorted-keys assertion for T13.4-1 (every JSON object's keys byte-sorted, shape- and value-blind). Add S-5: each adapter rejects synthetic wrong-shape documents rather than defaulting. Cites: §0 H-3, §17 S-5, T10.1-4, T13.4-1. Verify: S-5 passes.
- [ ] **HARNESS-05 — TypeScript tooling driver + S-4 self-test.** Implement the consumer-side driver per IMPLEMENTATION.md: compiles fixture consumer TS programs with the `typescript` compiler API and asserts diagnostics with their locations; runs compiled consumers under plain Node with no xspec runtime dependency (SPEC 13.1); queries go-to-definition targets and hover text via the language-service API. Add S-4: against a hand-written non-xspec fixture project in `test/fixtures/`, detect a known type error, a known definition location, and a known hover text. Cites: TEST-SPEC §4 preamble, §17 S-4; IMPLEMENTATION.md (harness). Verify: S-4 passes.
- [ ] **HARNESS-06 — Certification runner.** Implement the §17 runner: executes the full suite or a named subset of test IDs against an arbitrary fixture product supplied as an executable/workspace binding and nothing else — the same registry and driver product-facing tests use (C-2, one code path). Output must attribute pass/fail per test ID per fixture so certification results are legible in CI output (C-1). Cites: §17 preamble, C-1, C-2. Verify: runner executes a trivial named subset against the stub product and reports the expected diagnosed failures.
- [ ] **HARNESS-07 — Empty-stub fixture + S-7 red-green sweep.** Add a harness-owned empty-stub product under `test/fixtures/` (every command exits with an unexpected code and no output) and the S-7 self-test: run every product-facing test in the registry against that stub via the certification runner; assert every one fails with a diagnosed assertion failure and the sweep completes with no harness error, hang, or false pass. Deliberate choice: the sweep targets the harness-owned stub, not `src/`'s placeholder, so S-7 stays green after the product is implemented in Phase 10 (H-8 sanctions "a deliberately empty stub"). Cites: §0 H-8, §17 S-7. Verify: S-7 passes and keeps passing as later tasks add tests.
- [ ] **HARNESS-08 — Markdown oracle + S-6 vectors.** Implement the in-harness Markdown-compilation oracle for P-2: the removal/replacement/line-drop/line-terminator rules of SPEC.md §3 (as tested by T3-1…T3-5), including 1.4 class boundaries (U+00A0/U+0085/U+2028 not whitespace; lone CR a terminator). Add its S-6 fixed vector suite derived from SPEC.md §3's examples and rules, passing before any property test trusts it. Cites: §16 P-2, §17 S-6. Verify: S-6 Markdown vectors pass.
- [ ] **HARNESS-09 — Glob/capture oracle + S-6 vectors.** Implement the in-harness glob and capture matching oracle for P-7: SPEC.md §7 grammar (`*`, `?`, `**`, byte-wise case-sensitive, dot-segment rule, every other character literal) and §7.5 captures (never `/`, never empty, left-to-right shortest match). Add its S-6 fixed vector suite derived from SPEC.md §7/§7.5 examples. Cites: §16 P-7, §17 S-6. Verify: S-6 glob/capture vectors pass.
- [ ] **HARNESS-10 — Property-test infrastructure.** Implement seeded, reproducible generators with the failing seed reported on failure (H-10), shrinking on failure (§16), a fixed seed set used in CI, and an optional randomized local mode that reports seeds (E-5). Cites: §0 H-10, §16 preamble, §18 E-5. Verify: a demo property run under a fixed seed is reproducible; a forced failure reports its seed and shrinks.

## B. Product-facing suite (`test/suite/`) — expected red against the stub

Each task implements exactly the named TEST-SPEC.md tests, to their full text (arms included). Default verification per Conventions.

- [ ] **SUITE-01 — §1.1–1.2:** T1.1-1, T1.1-2, T1.1-3, T1.2-1, T1.2-2, T1.2-3 (requirement sections, tag equivalence, self-closing leaves, implicit root; T1.2-2 consumes the generated module — needs HARNESS-05).
- [ ] **SUITE-02 — §1.3:** T1.3-1…T1.3-6 (missing id 14.1, structural rule 14.2, level skipping, top-level segment count, duplicate IDs 14.3, 14.1-masks-14.2). CONF-VALID in-scope: keep fixtures within CERTIFICATIONS.md §CONF-VALID's scope (single spec group of `.mdx` with `id`/`tags` only; T1.3-5's cross-file arm multi-file).
- [ ] **SUITE-03 — §1.4:** T1.4-1…T1.4-4 (segment validity matrix incl. exact control/whitespace representatives, boundary code points U+00A0/U+0085/U+2028 valid, non-identifier segments via bracket access — needs HARNESS-05 — and tag validity). CONF-VALID in-scope (T1.4-1, T1.4-2, T1.4-4); byte-exact staging via HARNESS-01 so 14.4 (not 14.20) is at stake.
- [ ] **SUITE-04 — §1.5:** T1.5-1…T1.5-3 (workspace-relative `/`-separated identities from any cwd; `#`/non-UTF-8 discovered paths → 14.19, Linux-leg staging; `path#id` vs bare `path` addressing). T1.5-1/T1.5-3 rerun on the Windows leg via CI-01.
- [ ] **SUITE-05 — §1.6–1.7:** T1.6-1…T1.6-5, T1.7-1 (own vs subtree text byte anchors with the emission-independence parity arm; N+1 run counting; expansion everywhere; ownHash vs embedded-target edits; 14.20 encoding; zero-based byte-offset source ranges incl. self-closing and root arms).
- [ ] **SUITE-06 — §2.1:** T2.1-1…T2.1-5 (valid imports, specifier-form and binding-form 14.15 matrices, unused import records no edges, import cycles 14.9).
- [ ] **SUITE-07 — §2.2–2.3:** T2.2-1…T2.2-5, T2.3-1, T2.3-2 (`d` forms, module-as-target, duplicate collapse, empty array metadataHash parity, not rendered; `text(...)` embedding byte-asserted with `embeds` edge, string form, root targets).
- [ ] **SUITE-08 — §2.4:** T2.4-1…T2.4-4 (static accepted forms; dynamic forms → 14.8 in `d` and `text(...)`; arity; segment-exact computed access with 14.5/14.6/14.7 arms and a TS type-error arm — needs HARNESS-05).
- [ ] **SUITE-09 — §2.5–2.6:** T2.5-1…T2.5-3, T2.6-1…T2.6-3 (coverage attribute default/none/values 14.17; tag splitting, duplicate/empty collapse to omitted metadataHash, tag semantics). CONF-VALID in-scope: T2.6-1, T2.6-2.
- [ ] **SUITE-10 — §2.7:** T2.7-1…T2.7-3 (foreign constructs 14.16; comment removal with hash/category stability arms and the emptied-line boundary; props matrix 14.17/14.8 with the positive quoting arm).
- [ ] **SUITE-11 — §3:** T3-1…T3-6 (removals, replacement, line-drop rule with all counter-cases and class boundaries, line terminators incl. lone CR and no final terminator, in-line tags, emission scope). All but T3-6 run `markdown: { emit: true }` and byte-assert. CONF-MD in-scope, staging constraints (CERTIFICATIONS.md §VIOL-MD-CLASS/§VIOL-MD-CR): U+00A0/U+0085/U+2028 appear on removal-affected lines only in T3-3's class-boundary arms, and lone U+000D appears only in T3-4's fixtures — keep both out of every other in-scope fixture.
- [ ] **SUITE-12 — §4 preamble:** T4-1…T4-4 (generated-module header; the full TS import-rule 14.15 matrix incl. re-export forms, `import =`, dynamic import, derived-path specifiers, duplicate-binding collisions; aliased bindings; type-only imports record nothing). Needs HARNESS-05.
- [ ] **SUITE-13 — §4.1–4.2:** T4.1-1…T4.1-3, T4.2-1…T4.2-4 (skeleton chains and type errors, readonly, opaque node values; doc comments with expanded own text, 1000-code-point truncation, `*\/` escaping, go-to-definition/hover — needs HARNESS-05 language-service queries).
- [ ] **SUITE-14 — §4.3–4.4:** T4.3-1, T4.3-2, T4.4-1, T4.4-2 (`text(node)` runtime value + `embeds` edge; string/dynamic args → 14.8; cross-module branding type error 14.11 with runtime throw naming both modules; aliased `text` per module).
- [ ] **SUITE-15 — §4.5:** T4.5-1…T4.5-7 (marker semantics and runtime harmlessness, root markers, static rule 14.8, shadowing, sanctioned-uses 14.18 matrix, `text` statement position, type-level freedom).
- [ ] **SUITE-16 — §4.6:** T4.6-1…T4.6-4 (attribution matrix over all named-unit forms incl. static blocks, plain property initializers, dotted namespaces; anonymous default; not-named-units incl. private members; duplicate chains `@2`).
- [ ] **SUITE-17 — §5.2–5.3:** T5.2-1, T5.3-1, T5.3-2 (all four edge kinds with duplicate collapse; cycle detection matrix with full cycle paths at `check`; `build` rejects dependency cycles 14.9).
- [ ] **SUITE-18 — §5.4:** T5.4-1, T5.4-2 (reintroduced identity with the journaled variant and both journal-position arms; spelling never hashes).
- [ ] **SUITE-19 — §5.5:** T5.5-1…T5.5-6 (hash reporting/determinism; ownHash change matrix incl. the line-drop-toggle arm and the child-vs-embedding kind distinction; subtreeHash; effectiveHash per-edge pairs incl. the dedup-discriminating arm; metadataHash order-insensitivity; purity link).
- [ ] **SUITE-20 — §5.6:** T5.6-1…T5.6-6 (`impact --base` category matrix against committed git baselines: leaf edit worked example, child add/remove, d-target edit, metadata-only, multiple flags, added/deleted convention).
- [ ] **SUITE-21 — §6.1:** T6.1-1…T6.1-3 (journal lifecycle: absent after fresh `build`, appears on first rename/move, append-only byte-prefix, read commands never modify; entry determinism across identical directories; integrity 14.13 for garbage lines and dir/symlink-occupied path). CONF-CORE in-scope: T6.1-1, T6.1-2 — keep their fixtures within §CONF-CORE's scope (one spec group of importless `.mdx`; no `code`/`markdown`/`coverage`/`policy`; no git; mutating commands only `rename`, file-form `move`, mutating `review` with `create --strategy audit`).
- [ ] **SUITE-22 — §6.2:** T6.2-1…T6.2-4 (rename purity full-workspace hash sweep + empty impacted-code arms via journal unification; file-move purity; section-move impurity with clean and impure boundaries; same-parent final-position move). Git baselines via HARNESS-01.
- [ ] **SUITE-23 — §6.3:** T6.3-1…T6.3-4 (config-at-ref; absent journal both directions; replay composition; failure matrix at exit 2 incl. prefix violation, garbage current-journal line at `impact --base` and `review create --base` modifying nothing, invalid baseline sources, unresolvable ref, and the baseline-before-validation precedence arm).
- [ ] **SUITE-24 — §6.4 + §6.6:** T6.4-1…T6.4-7, T6.6-1 (rename rewrites everywhere; minimal byte edits; validation refusals modify nothing; usage-error ordering and unparseable-origin masking; type-level references untouched; valid-workspace precondition; finishing regeneration byte-identical to fresh build; manual restructuring is discontinuity).
- [ ] **SUITE-25 — §6.5:** T6.5-1…T6.5-6 (file form; section-form byte-exact edit rules incl. self-closing arms; re-identification and import add/remove exactness with byte determinism; refusal matrix modifying nothing; usage errors; identity-terms arms incl. refused exact self-move with journal compare).
- [ ] **SUITE-26 — §7 basics:** T7-1…T7-3 (config location/upward search/`--config`/none → 14.14 exit 2; declarative-form matrix; keys matrix with omitted/empty/unknown arms and their stated observations).
- [ ] **SUITE-27 — §7 discovery:** T7-4…T7-6 (glob semantics incl. byte-wise `é.mdx` probe, dot-segment rule, outside-root 14.14, literal `[1]`/`{a,c}`/`!`/`+(x)`; symlink non-discovery/non-traversal/cycle-no-hang; derived-file exclusion, import-adds-nothing, zero-source groups). CONF-DISC in-scope, staging constraints (§CONF-DISC, §VIOL-DISC-DERIVED): T7-6's exclusion arms staged over spec groups with `code` the empty map; T7-4/T7-5 fixtures stage no `.xspec.`-bearing names, no pattern naming `.xspec/`, and leave `markdown` absent. T7-4's `SPECS/*.mdx` single-casing probe reruns on the Windows leg (CI-01).
- [ ] **SUITE-28 — §7.1–7.3:** T7.1-1, T7.2-1, T7.3-1 (file in two spec groups; spec∩code 14.14; markdown config matrix incl. `emit` required, `outDir`, and the classification-by-configuration-alone arm).
- [ ] **SUITE-29 — §7.4–7.5:** T7.4-1, T7.4-2, T7.5-1…T7.5-6 (profile validation 14.14 matrix and semantics; rule validation matrix; forbidden/allowedOnly findings; selectors; captures incl. shortest-match determinism; build-vs-check 14.12).
- [ ] **SUITE-30 — §8:** T8-1…T8-5, T8.2-1 (direct/transitive modes, boundaries, boundary∩target overlap, root path exclusion with its coverage-scoped-exclusion arms, report contract incl. tie-break, ignored-reason order, `--check`, `--json` parity).
- [ ] **SUITE-31 — §9 through 9.2:** T9-1, T9.1-1, T9.2-1…T9.2-5 (baseline plumbing; categories equal 5.6; directly/transitively impacted; deleted location; category independence; journal-mapped impact edges — the positive counterpart of T6.2-1).
- [ ] **SUITE-32 — §9.3:** T9.3-1…T9.3-3 (ancestor collapsing; witness edge/path minimization incl. metadata terminus and `embeds`-over-`references` preference; deletion identities incl. the twice-reported reintroduced case).
- [ ] **SUITE-33 — §10.1:** T10.1-1…T10.1-4 (session storage exact file set and determinism; name validity/case rules with `create`'s ASCII-case-insensitive refusal; non-session files ignored; the corruption matrix staged via HARNESS-04's adapter transformations — never fabricating a session file). T10.1-2/T10.1-3 single-casing probes rerun on the Windows leg (CI-01).
- [ ] **SUITE-34 — §10.2–10.3:** T10.2-1…T10.2-4, T10.3-1, T10.3-2 (item fields; baseline fixing; actionability after loss; `current` reported-as-recorded with creation and presence arms; statuses; re-blocking).
- [ ] **SUITE-35 — §10.4:** T10.4-1…T10.4-5 (per-kind relevant-hash sensitivity with controls; presence changes; context-set change; rename immunity with order-flip and reintroduction arms; reads never write — byte-identical session across reads computing invalidation). CONF-CORE in-scope: T10.4-5 — stage within §CONF-CORE's scope (audit-strategy sessions).
- [ ] **SUITE-36 — §10.5:** T10.5-1…T10.5-6 (path-blocks generation per SPEC §15's worked change; blocking chains; metadata/dependency/code items incl. the added-target exclusion; total item order; re-derivation on `updated` with decomposition recursion; baseline recording).
- [ ] **SUITE-37 — §10.6:** T10.6-1…T10.6-3 (audit generation git-less; order and blocking incl. absent-scope ordering; re-derivation on `updated` with `no-change` control).
- [ ] **SUITE-38 — §10.7 commands I:** T10.7-1…T10.7-6 (create flag exclusivity; recorded parameters; unresolvable baseline; coverage sessions; `list` byte-order/stored-status/corrupt contract; `status`).
- [ ] **SUITE-39 — §10.7 commands II:** T10.7-7…T10.7-12 (`next` incl. fully-resolved and self-contained payload; `show`/`export`; `split` semantics incl. id retirement across strategies; `resolve`; coverage re-derivation; the full payload text contract byte-asserted per kind incl. absent-node provenance).
- [ ] **SUITE-40 — §11:** T11-1…T11-7 (JSON-only query surface via adapters: node, nodes filters, subtree/ancestors row contract, edges, reachable incl. zero-length-path and `--kinds contains` exit 2, identity resolution, ordering determinism).
- [ ] **SUITE-41 — §12.0 I:** T12.0-1…T12.0-6 (`--json` everywhere sweep; stream separation; `--config` resolution; flag repetition; argument addressing incl. `\`-separator negative and non-UTF-8 argv Linux arm; case-and-bytes probes). T12.0-5 (less its Linux-only arm) and T12.0-6's probe rerun on the Windows leg (CI-01).
- [ ] **SUITE-42 — §12.0 II:** T12.0-7…T12.0-12 (determinism sweep incl. irrelevant-env variation; shortest-path tie-break per command; exit-code partition table; check ordering cross-refs; git read-only byte-compare of `.git/` around every git-reading invocation; git-less operation sweep).
- [ ] **SUITE-43 — §12.1–12.2:** T12.1-1…T12.1-4, T12.2-1…T12.2-3 (build products; no-policy; regeneration and orphan removal; failed build modifies nothing; check green path; per-family finding scope; check never refreshes).
- [ ] **SUITE-44 — §12.3–12.5:** T12.3-1, T12.3-2, T12.4-1, T12.5-1 (ids ordering/`--tree`/restricted-tree/`--file`; `--unreferenced` vs uncovered; show's full enumeration adapter-compared to `query node`; dispatch and unknown-command exit 2).
- [ ] **SUITE-45 — §13.1–13.2:** T13.1-1, T13.1-2, T13.2-1 (generated layout and orphan recording; standalone consumer project under standard tooling only — the section-4 umbrella E2E, needs HARNESS-05; Markdown emission placement incl. `outDir`).
- [ ] **SUITE-46 — §13.3:** T13.3-1…T13.3-4 (serving reads; refresh semantics with the T13.3-2 operational deletion definition; failed refresh — every read and mutating `review` subcommand reports validation errors, exit 1, modifies nothing; graph-data byte determinism).
- [ ] **SUITE-47 — §13.4:** T13.4-1…T13.4-7 (plain committable files + sorted-keys assertion via HARNESS-04; derived reproducibility; orphan knowledge boundary; derived paths belong to xspec incl. symlink-occupant replacement; durable protection; symlink write-refusal rules 14.22/14.13/14.21 with the above-root positive arm; source exclusion deferred to T7-6). CONF-CORE in-scope: T13.4-5 — staging constraint (§VIOL-CORE-PERSISTREADS): its fixture sessions contain no stale resolution when its `build`-and-read byte-compares run.
- [ ] **SUITE-48 — §13.5:** T13.5-1…T13.5-7 (hold-seam basics incl. occupied-path exit 2 and non-mutating-command unknown-flag arm; mutual exclusion; exclusivity ends with the process; readers during mutation + build/query storm; atomic visibility via polling reader; workspace isolation; interrupted mutation with kill-timing spread and the disjunctive check assertion). Uses HARNESS-02's hold/kill/concurrency support. CONF-CORE in-scope: T13.5-1…T13.5-5. Staging constraints: every mutating command these tests drive is `rename`, file-form `move`, or a mutating `review` subcommand with `create --strategy audit` (§CONF-CORE); T13.5-2's excluded commands carry no `--test-hold`, and its modifies-nothing compare brackets the excluded command alone with the snapshot taken while command 1 is already held (§VIOL-CORE-NOLOCK, §VIOL-CORE-EARLYWRITE); T13.5-3's subsequent mutating command succeeds whether or not the killed operation's writes landed — not a retry of the killed operation (§VIOL-CORE-EARLYWRITE).
- [ ] **SUITE-49 — §14:** T14-1…T14-5 (multi-error completeness with file/location/correction info; unresolved references 14.5/14.6/14.7; masking incl. configuration-error suppression; reporter matrix build vs check vs review; grammar selection `.tsx`/`.ts`/`.mts`).
- [ ] **SUITE-50 — §15:** T15-1 (SPEC.md §15's exact workspace: six edges, transitive coverage path, edit categories, four path-blocks items, rename + no-change impact — one integration test).

## C. Property-based and fuzz tests (`test/suite/`) — expected red against the stub

- [ ] **PROP-01 — P-1 segment/tag validity.** Generator weighted toward 1.4 boundaries (whitespace/control classes, U+00A0/U+0085/U+2028, `.`/`#`, forbidden names, glob metacharacters as ordinary valid characters); accepted-iff-valid for segments; tags via the 2.6 splitting model (tokens each valid; zero tokens = omitted). Cites §16 P-1, H-10. CONF-VALID in-scope. Needs HARNESS-10.
- [ ] **PROP-02 — P-2 + P-3 Markdown/text algebra.** Random documents (prose, nested sections, imports, single/multi-line comments, embeddings, mixed terminators, boundary-code-point-weighted content) compiled output equals the HARNESS-08 oracle; determinism; byte preservation outside removals. P-3: root subtree text = compiled output; run interleaving; N+1 runs. Cites §16 P-2/P-3. CONF-MD in-scope.
- [ ] **PROP-03 — random-workspace generator + P-4 hash laws.** Seeded generator of random valid workspaces and random single edits; assert the 5.5 iff-conditions for all four hashes and identical-workspace hash identity. Cites §16 P-4.
- [ ] **PROP-04 — P-5 + P-6 purity and replay.** Random journaled rename/file-move sequences: hashes byte-stable, impact empty against every prior commit, references resolve; random section moves: only predicted parents categorized. P-6: random edit/rename/move/commit interleavings — impact categories against each historical baseline equal an oracle graph diff mapped through the journal suffix. Cites §16 P-5/P-6.
- [ ] **PROP-05 — P-7 glob/capture property.** Random patterns/paths over the §7/§7.5 grammar (generators include foreign-dialect metacharacters); product match decisions and capture values equal the HARNESS-09 oracle; uniqueness under shortest-match; captures never span `/` or match empty. Cites §16 P-7.
- [ ] **PROP-06 — P-8 parser robustness.** Fuzzed bytes (mutated MDX/TS/config, invalid UTF-8, BOMs, giant nesting, pathological terminators): every command terminates, never emits partial JSON under `--json`, exits only 0/1/2, and failing `build`s modify nothing. Cites §16 P-8.
- [ ] **PROP-07 — P-9 review invariants.** Random valid review-operation sequences interleaved with edits: ≤1 item per kind+scope; `blockedBy` acyclic; retired ids never reused; `next` contract; reads never change bytes; sessions re-read non-corrupt. Cites §16 P-9.
- [ ] **PROP-08 — P-10 concurrency.** Randomized schedules of concurrent readers plus one mutating command (via `--test-hold` and kills): readers observe only prior-or-complete states; post-hoc journal-line and session-status accounting loses nothing. Cites §16 P-10. Uses HARNESS-02 choreography.

## D. Certification fixtures and the certification run (`test/fixtures/` + `test/self/`)

Fixtures are harness-owned executable products driven only through the C-2 executable/workspace binding; they never import product code. Each conformer implements its entry's **Scope** with the simplest conforming behavior; each violator is its conformer plus exactly its one **Deviation** (sharing conformer code internally is fine — the deviation must be the only behavioral difference). Fixture executables must be invocable in CI without network; if a build step is added, record it in `AGENTS.md`. Verification per fixture task = a certification-runner invocation of the fixture's in-scope tests (so the in-scope suite/prop tasks must land first; conformer tasks list them).

- [ ] **CERT-01 — CONF-CORE conformer.** Per CERTIFICATIONS.md §CONF-CORE: importless single-spec-group `.mdx` workspaces; `build`; the 13.3 read commands per 12.0 (`check`, `ids`, `show`, `query`, `coverage` zero profiles, `review` reads; `impact --base` = exit-2 unreadable baseline); `rename` + file-form `move` with journal append (6.1/6.2); `review` audit strategy through `create`/`resolve`/`split`/reads with 10.4 read-time invalidation; 13.4 durable protection; 13.5 in full incl. `--test-hold`. In-scope: T6.1-1, T6.1-2, T10.4-5, T13.4-5, T13.5-1…T13.5-5 (SUITE-21/35/47/48). Verify: all nine pass against it via HARNESS-06.
- [ ] **CERT-02 — VIOL-CORE-NOLOCK.** Deviation: mutating commands do not exclude one another (hold still created/honored). Certifies T13.5-2. Verify: exactly T13.5-2 fails, other eight in-scope pass.
- [ ] **CERT-03 — VIOL-CORE-EARLYWRITE.** Deviation: modifications performed before hold-file creation. Certifies T13.5-1, T13.5-4. Verify: exactly those fail, others pass.
- [ ] **CERT-04 — VIOL-CORE-STALELOCK.** Deviation: exclusivity not released by abnormal termination. Certifies T13.5-3. Verify: exactly it fails.
- [ ] **CERT-05 — VIOL-CORE-PARTIALWRITE.** Deviation: derived-file writes expose a sustained strict-prefix interval. Certifies T13.5-5. Verify: exactly it fails.
- [ ] **CERT-06 — VIOL-CORE-CHATTYREADS.** Deviation: `build` and non-refused read commands append one fixed journal line. Certifies T6.1-1, T13.4-5. Verify: exactly those fail.
- [ ] **CERT-07 — VIOL-CORE-PERSISTREADS.** Deviation: review reads persist computed invalidation to the session file. Certifies T10.4-5. Verify: exactly it fails.
- [ ] **CERT-08 — CONF-VALID conformer.** Per §CONF-VALID: `build` with 14.1–14.4 reporting (file, location, condition identity, 14.2 expected form, exit codes) and `query node`/`query nodes` reporting identity, tags, metadataHash, over `.mdx` workspaces with `id`/`tags` props. In-scope: T1.3-1…6, T1.4-1/2/4, T2.6-1/2, P-1 (SUITE-02/03/09, PROP-01). Verify: all twelve pass.
- [ ] **CERT-09 — VIOL-VALID-CTRL.** Deviation: non-whitespace control characters accepted in segments and tags. Certifies T1.4-1, T1.4-4, P-1. Verify: exactly those fail.
- [ ] **CERT-10 — VIOL-VALID-WIDE.** Deviation: U+00A0/U+0085/U+2028 treated as whitespace for 1.4 validity. Certifies T1.4-2, T1.4-4, P-1. Verify: exactly those fail.
- [ ] **CERT-11 — CONF-MD conformer.** Per §CONF-MD: byte-exact Markdown per SPEC §3 (removal, replacement, line-drop, terminators), `query node` own/subtree text (1.6), emission scope 7.3, over spec-group workspaces with imports, embeddings, comments, mixed terminators, full 2.7 prop set. May share HARNESS-08's compilation logic. In-scope: T3-1…T3-6, P-2, P-3 (SUITE-11, PROP-02). Verify: all eight pass.
- [ ] **CERT-12 — VIOL-MD-CLASS.** Deviation: U+00A0/U+0085/U+2028 classified whitespace by the line-drop rule (consistently in output and text values). Certifies T3-3, P-2. Verify: exactly those fail.
- [ ] **CERT-13 — VIOL-MD-CR.** Deviation: lone U+000D not a line terminator (consistently). Certifies T3-4, P-2. Verify: exactly those fail.
- [ ] **CERT-14 — CONF-DISC conformer.** Per §CONF-DISC: `build` + `ids` as the discovery observation; glob semantics of §7 (literals outside `*`/`?`/`**`, byte-wise case-sensitive, dot-segment rule); symlink refusal; 13.4 source exclusion; 14.14 outside-root; 14.15 import errors; zero-source groups and empty maps. May share HARNESS-09's matcher. In-scope: T7-4, T7-5, T7-6 (SUITE-27). Verify: all three pass.
- [ ] **CERT-15 — VIOL-DISC-DIALECT.** Deviation: `[ ]`/`{ }` active metacharacters (one rule of §7 broken). Certifies T7-4. Verify: exactly it fails.
- [ ] **CERT-16 — VIOL-DISC-SYMLINK.** Deviation: discovery follows file symlinks (broken links still ignored, directory links untraversed). Certifies T7-5. Verify: exactly it fails, by assertion not hang.
- [ ] **CERT-17 — VIOL-DISC-DERIVED.** Deviation: 13.4 source exclusion not applied to glob matches. Certifies T7-6. Verify: exactly it fails on its exclusion arms.
- [ ] **CERT-18 — Certification self-test wiring (C-1 gate).** Add the `test/self/` certification test that, via HARNESS-06, runs every CERTIFICATIONS.md fixture: conformer → all in-scope tests pass; each of the 13 violators → exactly its certified tests fail and every other in-scope test passes. Results legible per fixture/test in the `harness-self` CI job output (`npm run test:self`; `.github/workflows/ci.yml` already runs it network-disabled). Depends on CERT-01…CERT-17 and their in-scope test tasks. Cites §17 C-1/C-2. Verify: `npm run test:self` green locally and in CI.

## E. Traceability, Windows leg, cleanup

- [ ] **TRACE-01 — H-7 traceability map + S-1 self-check.** Author the machine-readable test-ID → SPEC.md-passage map covering every test in the harness. Keys (all must be mapped by ≥1 test): SPEC.md's unnumbered preamble; every numbered subsection; the section bodies of 3, 4, 5, 7, 8, 9, 10, 11, 14, 15. S-1 fails on any unmapped key, on a test mapping to a nonexistent key, and — conservative extension — on a map entry whose test ID has no implemented test or an implemented product-facing test missing from the map. Schedule after the SUITE/PROP tasks (S-1 goes green only when the suite is complete). Cites §0 H-7, §17 S-1. Verify: S-1 passes via `npm run test:self`.
- [ ] **CI-01 — E-6 Windows subset + Linux-side exchange writer.** Implement in `test/windows/`: T1.5-1, T1.5-3, T12.0-5 (less its Linux-only non-UTF-8 arm), and the single-casing probes of T10.1-2, T10.1-3, T12.0-6, T7-4; plus the representative byte-identity fixture (build, check, query, coverage, impact, journaled `rename`, journaled file-form `move` — the specifier-computation probe with post-move clean `check` — and an audit review session: `create --strategy audit`, `next --json`, a `resolve`, `export`), asserting reports, move-rewritten sources, generated files, emitted Markdown, graph data, journal, and session file byte-identical to the Linux leg's outputs read from `XSPEC_E6_EXCHANGE_DIR`. Implement the Linux-side writer (runs in the suite project, writes the same fixture's outputs into `XSPEC_E6_EXCHANGE_DIR` when set — see `.github/workflows/ci.yml` artifact plumbing). Git fixture scripted with pinned platform-independent author/committer/timestamps (HARNESS-01). The comparison fails loudly when the exchange dir is missing while the product exists; all these tests are product-facing (red against the stub is expected, as diagnosed failures — no crash on either leg). Cites §18 E-6, H-8. Verify: `npm run test:windows` collects the subset and fails-as-diagnosed against the stub.
- [ ] **CLEAN-01 — Scaffold removal + phase-gate confirmation.** Remove the three scaffold smoke tests (`test/self/scaffold.smoke.test.ts`, `test/suite/scaffold.smoke.test.ts`, `test/windows/scaffold.smoke.test.ts` — each self-labeled for removal) once their projects hold real tests. Confirm the phase gate: `npm run test:self` green twice consecutively with identical results (E-5, fixed seeds); the self project passes under parallel execution (E-3, H-1); `harness-self` CI job green with certification results in its output (C-1); `suite-linux` and `suite-windows` red is the expected pre-product baseline (product-facing tests fail as diagnosed assertions only — audit for any crash/hang/false-pass and fix in the harness). E-1's full-suite content (sections 1–17 incl. certification) exists and runs network-disabled via the existing scripts. Cites §17, §18 E-1/E-3/E-5, H-8/H-9. Verify: as stated; then this file's last task is removed and the plan file deleted per PROCESS.md Ralph rules.
