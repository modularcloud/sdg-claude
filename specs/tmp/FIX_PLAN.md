# FIX_PLAN — Phase 9: implement the test harness

Goal: harness adherence to `specs/TEST-SPEC.md` + `specs/CERTIFICATIONS.md`. All harness self-tests (TEST-SPEC §17 S-1…S-7) and all certifications (C-1 over every CERTIFICATIONS.md fixture) must pass locally (`npm run test:self`) and in the `harness-self` CI job. Product-facing tests are expected to FAIL at this phase — the product is a stub. Product code (`src/`) must not be touched.

Planned 2026-07-10 from the Phase 9 iteration-1 compliance panel (TEST-SPEC.md and CERTIFICATIONS.md scopes) and the red verify run at HEAD 89a52bb. Baseline: zero of the ~260 specified tests implemented; `test/helpers/` and `test/fixtures/` empty; only three self-labeled scaffold smoke tests exist.

## Conventions binding every task (read before starting any task)

- **Scope guard.** Phase 9: edit only harness code (`test/`), `AGENTS.md` (build/lint/run knowledge only), and this file. Never modify `src/`, `specs/` documents, or `.claude/`. The harness never imports product code; it drives the built `xspec` executable (`dist/cli/bin.js`, see `AGENTS.md`) strictly as a subprocess (TEST-SPEC H-2; IMPLEMENTATION.md).
- **Layout** (per `test/vitest.config.ts` comments): shared machinery in `test/helpers/`; product-facing tests (§§1–16) in `test/suite/`; self-tests and the certification runner (§17) in `test/self/`; fixture products and consumer fixture projects in `test/fixtures/`; the E-6 subset in `test/windows/`; `test/local/` stays empty (E-2).
- **One code path (C-2).** Every product-facing test body must be written against a *product binding* (executable path + workspace dir + env), defaulting to the built product, so the certification runner can execute the identical test — addressable by its TEST-SPEC ID, individually and as named subsets — against a fixture executable. No test hard-codes the product path. Keep a registry keyed by test ID; the Vitest files in `test/suite/` are thin wrappers over it.
- **Red-green discipline (H-8).** Against the current stub product (exits 86, no output) every product-facing test must fail with a *diagnosed assertion failure* — never a harness crash, hang, or pass. Definition of done for a `SUITE-*`/`PROP-*` task: tests implemented per their TEST-SPEC text; `npm run typecheck` and `npm run format:check` green; each new test observed failing-as-diagnosed against the stub (run the suite project filtered to the new files); no `.skip` anywhere (H-9).
- **Assertions.** Byte equality wherever SPEC.md fixes bytes; opaque content asserted only by its stated observable contract (H-4). Every test asserts the exact exit code and, where relevant, stdout/stderr separation (H-5). Determinism tests use the run-twice / two-directory protocol (H-6). No wall clock or randomness in assertions (H-10).
- **Violator count.** CERTIFICATIONS.md defines 4 conformers and **13** violators (6 CORE, 2 VALID, 2 MD, 3 DISC). The iteration-1 findings' "12" is superseded by the document itself.
- **Staging constraints** (CERTIFICATIONS.md preamble ¶3) bind the harness's fixtures for named tests and are restated inside the tasks below for T13.5-2, T13.5-3, T13.4-5, T3-1…T3-6, T7-4, T7-5, T7-6. Do not stage those tests differently.
- If a task teaches you anything new about building, linting, or running the code (e.g. a fixture build step), record it in `AGENTS.md`.

---

## D. Certification fixtures and the certification run (`test/fixtures/` + `test/self/`)

Fixtures are harness-owned executable products driven only through the C-2 executable/workspace binding; they never import product code. Each conformer implements its entry's **Scope** with the simplest conforming behavior; each violator is its conformer plus exactly its one **Deviation** (sharing conformer code internally is fine — the deviation must be the only behavioral difference). Fixture executables must be invocable in CI without network; if a build step is added, record it in `AGENTS.md`. Verification per fixture task = a certification-runner invocation of the fixture's in-scope tests (so the in-scope suite/prop tasks must land first; conformer tasks list them).

Landed so far (2026-07-16, CERT-01…CERT-07): the CONF-CORE conformer lives at `test/fixtures/conf-core/` — `bin.mjs` (entry) + `product.mjs` (plain Node ESM, no build step). The deviation seam is concrete: `runXspec(argv, cwd, options)` assigns `options` to product.mjs's module-level `deviations` switches (all off = conformer behavior); each violator adds a `bin-<name>.mjs` entry passing exactly one switch — VIOL-CORE-NOLOCK is `bin-nolock.mjs` + `noMutualExclusion`, consumed in `runMutating` (exclusivity neither acquired nor checked; hold seam unchanged); VIOL-CORE-EARLYWRITE is `bin-earlywrite.mjs` + `writesBeforeHold`, also consumed in `runMutating` (the operation runs to completion before the hold seam; exclusivity acquisition unchanged); VIOL-CORE-STALELOCK is `bin-stalelock.mjs` + `staleLockBlocks`, consumed in `acquireExclusivity` (an existing lock file refuses outright — no holder-liveness detection or stale cleanup; normal completion still unlinks the lock in `release()`); VIOL-CORE-PARTIALWRITE is `bin-partialwrite.mjs` + `partialDerivedWrites`, consumed in `regenerate` — the fixture's only derived-file writer — via `writeFilePartialThenComplete` (the target path itself receives a strict prefix in place, holds it 50 ms — long against T13.5-5's ~1 ms polling cadence — then the remainder is appended; never unlinked, completed commands leave the conformer's final bytes; durable files — journal, sessions, sources — keep atomic writes); VIOL-CORE-CHATTYREADS is `bin-chattyreads.mjs` + `chattyReads`, consumed in `runXspec` after command dispatch (every non-exit-2 `build`/read invocation — for `review`, exactly the read subcommands — appends one fixed line via `appendJournal` to the root `loadConfig` resolved, tracked as `lastResolvedRoot`; the fixed line is deliberately well-formed under the fixture's own journal reader — a `rename` entry whose empty path/from/to maps nothing — so `check`'s 14.13 scan, identity mapping, and review reads stay conformer-identical and only journal bytes deviate; mutating commands unchanged); VIOL-CORE-PERSISTREADS is `bin-persistreads.mjs` + `persistReadInvalidation`, consumed by the four review reads (`status`/`next`/`show`/`export`) via `withReadInvalidationPersistence` — the body runs (report emitted conformer-identically) while `effectiveStatus`, the fixture's only `isStale` caller, collects exactly the resolved items whose invalidation the read computed; afterwards those stored statuses are rewritten to `invalidated` and the session file rewritten atomically, and a read computing no invalidation writes nothing (`review list` computes no invalidation and, like the mutating subcommands, is unchanged; `sessionInvariantsHold` admits stored `invalidated` under the switch so the deviating product's own persisted state stays loadable — refusing it as corrupt would be a second deviation). With CERT-06, T13.5-4's storm-arm clean-build compare excludes `.xspec/journal` (durable, not derived): CERTIFICATIONS.md's §VIOL-CORE-CHATTYREADS expected-failure analysis pins that the 13.5 tests assert hold, exclusion, and derived-file behavior, never journal bytes — journal never-modifies discrimination lives in T6.1-1/T13.4-5. Fixture bindings and ID sets live in `test/self/certification-fixtures.ts` (per violator: a binding + a `<NAME>_CERTIFIES` list); per-fixture verification tests in `test/self/certification.test.ts` — conformers go through its `verifyConformerAllPass` helper, violators through `verifyViolatorExpectedFailures` (asserts exactly the certified tests fail as diagnosed assertion failures and every other in-scope test passes; both print each runner report to stdout for C-1 CI legibility; CERT-18 still owns the whole-document gate). With CERT-08, the CONF-VALID conformer lives at `test/fixtures/conf-valid/` — same shape (`bin.mjs` + `product.mjs`, `runXspec(argv, cwd, options)` deviation seam, all switches off): a hand-rolled MDX-lite lexer takes quoted `id`/`tags` attribute values as their raw characters, so control bytes, exotic whitespace, and boundary code points reach 14.4 validation and never surface as 14.20; validation compares segment *sequences* for 14.2 (an empty segment is a 14.4 matter — `id=""` and `a.`→`a..b` are structurally sound, per T1.4-1's staging) and masks 14.2 for immediate children of an id-less section; findings carry the offending construct's own byte range; `build` writes nothing and queries recompute from sources; metadataHash is a SHA-256 of the collapsed sorted tag set, so 2.6-equivalent spellings hash identically and zero-token values hash as the omitted prop. With CERT-09, VIOL-VALID-CTRL is `bin-ctrl.mjs` + `acceptNonWhitespaceControls`, consumed in `valueViolation`'s control branch — the whitespace branch runs first, so exactly the non-whitespace controls (U+0000–U+0008, U+000E–U+001F, U+007F) are accepted in segments and tags while whitespace stays rejected in segments and `splitTags` is untouched; verified failures against the fixture: T1.4-1 (its U+0000 segment arm builds instead of exit 1), T1.4-4 (its U+0000 tag arm builds), P-1 (falsified under the fixed seed set — seed 271828183, segment property), every other §CONF-VALID in-scope test passing, 0 errors/hangs. With CERT-10, VIOL-VALID-WIDE is `bin-wide.mjs` + `widenValidityWhitespace`, consumed in `valueViolation`'s whitespace branch — U+00A0/U+0085/U+2028 treated as whitespace for 1.4 validity only, so segments and tags containing them are rejected with 14.4; tag splitting (`splitTags`) deliberately stays on the literal 1.4 whitespace class in every fixture (§VIOL-VALID-WIDE deviates validity, not splitting); verified failures against the fixture: T1.4-2 (its boundary segments are rejected instead of building), T1.4-4 (its U+00A0 boundary tag arm), P-1 (falsified under the fixed seed set — seed 271828183, segment property), every other §CONF-VALID in-scope test passing, 0 errors/hangs. With CERT-11, the CONF-MD conformer lives at `test/fixtures/conf-md/` — same shape (`bin.mjs` + `product.mjs`, `runXspec(argv, cwd, options)` deviation seam, all switches off). The 2026-07-14 remark-mdx caveat is resolved by construction, with no section-3.ts fixture fix: the fixture hand-lexes MDX-lite — imports at line start, `<S>`/`<Spec>` tags with the 2.7 prop set (quote-aware braced `d` included), single- and multi-line comments, `{text(...)}` with local string or external property-chain arguments — so T3-3's import-followed-by-non-blank-line and T3-1's `gamma` opening tag with trailing same-line content parse fine, and exotic bytes in content never surface as 14.20. Compilation is a port of the S-6-vetted oracle's line model (`test/helpers/oracles/markdown.ts`, per the entry's may-share note) extended with node attribution: every kept output atom — content chunk, expansion, line terminator — carries its owning node (the innermost section enclosing its source position; an expansion is owned by the embedder, 1.6), so emitted Markdown and `query node` own/subtree text come from one attributed compile and P-3's N+1-runs interleaving holds by construction. A same-file embedding reads its target's already-finalized atoms (in-scope staging closes targets before their embeds; anything else is a 14.9 finding), a cross-file embedding compiles the target file first (memoized recursion, cycle-detected), so chains expand fully bottom-up. `build` writes nothing but the emitted `.md` files under `markdown: { emit: true }` — nothing at all otherwise (7.3) — and queries recompute from sources (no graph data). CERT-12/CERT-13 hook points are pinned in product.mjs: `isDropWhitespaceCode` is the only whitespace class the compile consults (the drop rule's classification, §VIOL-MD-CLASS) and `terminatorAt` is the compile's whole line model (lone-CR recognition, §VIOL-MD-CR); both feed the single attributed compile, so a deviation there is automatically "consistently in Markdown output and, through 1.6, in own and subtree text". Verified: all eight §CONF-MD in-scope tests pass against the fixture through the certification runner (T3-1…T3-6, P-2, P-3 — 8 pass, 0 fail/error/hang). With CERT-12, VIOL-MD-CLASS is `bin-class.mjs` + `widenDropWhitespace`, consumed in `isDropWhitespaceCode` — U+00A0/U+0085/U+2028 additionally classified whitespace by the line-drop rule; the one predicate feeds both drop-rule decisions ("contained non-whitespace in the source" and "left empty or whitespace-only"), so the drop conjunction still implies a removal (a removal-free line holding only whitespace/boundary code points stays kept — every removable construct carries ASCII non-whitespace, verified by hand alongside the certification run) and, through the single attributed compile, Markdown output and 1.6 text values deviate consistently, preserving P-3's internal consistency per §VIOL-MD-CLASS's expected-failure analysis. Verified failures against the fixture: T3-3 (its class-boundary arms — lines left holding only the boundary code points after removals are dropped instead of kept; the emitted-bytes compare fails as a diagnosed assertion), P-2 (falsified under the fixed seed set — seed 271828183, trial 6 of 12; the oracle keeps such lines), every other §CONF-MD in-scope test passing (T3-1, T3-2, T3-4, T3-5, T3-6, P-3 — 6 pass, 2 fail, 0 error/hang), and the CONF-MD conformer re-verified 8/8 with the switch off. With CERT-13, VIOL-MD-CR is `bin-cr.mjs` + `loneCrNotTerminator`, consumed in `terminatorAt` — a lone U+000D (not followed by U+000A) is no terminator, staying an ordinary in-line character (still drop-rule whitespace), while CRLF and lone U+000A remain terminators; line extents, and with them the drop rule's decisions and consumed terminator bytes, diverge through the single attributed compile, so output and 1.6 text values deviate consistently and P-3's internal consistency is preserved per §VIOL-MD-CR's expected-failure analysis. Verified failures against the fixture: T3-4 (its lone-CR fixture compiles to `one\r\rbody\r\rtail` instead of `one\rbody\rtail` — the emitted-bytes compare fails as a diagnosed assertion), P-2 (falsified under the fixed seed set — seed 271828183, trial 1 of 12), every other §CONF-MD in-scope test passing (T3-1, T3-2, T3-3, T3-5, T3-6, P-3 — 6 pass, 2 fail, 0 error/hang), and the CONF-MD conformer re-verified 8/8 with the switch off. With CERT-14, the CONF-DISC conformer lives at `test/fixtures/conf-disc/` — same shape (`bin.mjs` + `product.mjs`, `runXspec(argv, cwd, options)` deviation seam, all switches off). The glob matcher is a port of the S-6-vetted oracle's discovery half (`test/helpers/oracles/glob.ts`, per the entry's may-share note and the oracle's own header): byte-wise over UTF-8 (`?` is one byte, so `?.mdx` rejects the two-byte `é.mdx` while `??.mdx`/`*.mdx` accept it), case-sensitive, dot-segment rule on the pattern as written (`**` never consumes a dot-initial segment), every character outside `*`/`?`/`**` a literal. Patterns resolve lexically against the config directory (`path.posix.normalize`; wildcard segments are ordinary names to resolution) — absolute or `..`-escaping patterns are refused 14.14 at load, exit 2/stderr/empty stdout. Discovery order: walk plain files (symlink entries neither discovered nor traversed, so cycles terminate) → match the union of all groups → 13.4 exclusion (`.xspec.` basenames, `.xspec/` paths, and — exactly under `markdown: { emit: true }` — the `.md` twins of the matched non-excluded `.mdx` candidates, by configuration alone, 7.3) → 14.19 on surviving non-`.mdx` or `#`-bearing matches. `build` on success writes the generated `X.xspec.ts` and (emit only) `X.md` beside each source with fixed trivial bytes — content beyond path out of scope — replacing occupants (13.4), which keeps T7-6's after-build listing arm non-vacuous; failing build writes nothing; `ids` recomputes from sources (no graph data), reporting validation findings exit 1, and answers `{files:[{file, ids}]}` in UTF-8-byte file order with document-order IDs. Imports: 2.1 single-default-binding form resolved against the importing file's directory; undiscovered target, `S`/`Spec`/`text` shadowing, or a duplicate binding → 14.15 at the import statement's byte range. CERT-15/16/17 hook points are pinned in product.mjs with their switch names: `dialectMetachars` (§VIOL-DISC-DIALECT) in `parseSegment` — the single place a pattern character acquires meaning; `followFileSymlinks` (§VIOL-DISC-SYMLINK) in `walkPlainFiles`' symbolic-link branch — file links to existing files discovered, broken links still ignored, directory links still untraversed, so T7-5 fails by assertion not hang; `noDerivedExclusion` (§VIOL-DISC-DERIVED) in `discoverSources`' exclusion filter — matches kept unexcluded, a non-`.mdx` occupant surfacing as 14.19. Verified: all three §CONF-DISC in-scope tests pass against the fixture through the certification runner (T7-4, T7-5, T7-6 — 3 pass, 0 fail/error/hang), full `npm run test:self` green (216/216).

- [ ] **CERT-15 — VIOL-DISC-DIALECT.** Deviation: `[ ]`/`{ }` active metacharacters (one rule of §7 broken). Certifies T7-4. Verify: exactly it fails.
- [ ] **CERT-16 — VIOL-DISC-SYMLINK.** Deviation: discovery follows file symlinks (broken links still ignored, directory links untraversed). Certifies T7-5. Verify: exactly it fails, by assertion not hang.
- [ ] **CERT-17 — VIOL-DISC-DERIVED.** Deviation: 13.4 source exclusion not applied to glob matches. Certifies T7-6. Verify: exactly it fails on its exclusion arms.
- [ ] **CERT-18 — Certification self-test wiring (C-1 gate).** Add the `test/self/` certification test that, via HARNESS-06, runs every CERTIFICATIONS.md fixture: conformer → all in-scope tests pass; each of the 13 violators → exactly its certified tests fail and every other in-scope test passes. Results legible per fixture/test in the `harness-self` CI job output (`npm run test:self`; `.github/workflows/ci.yml` already runs it network-disabled). Depends on CERT-01…CERT-17 and their in-scope test tasks. Cites §17 C-1/C-2. Verify: `npm run test:self` green locally and in CI.

## E. Traceability, Windows leg, cleanup

- [ ] **TRACE-01 — H-7 traceability map + S-1 self-check.** Author the machine-readable test-ID → SPEC.md-passage map covering every test in the harness. Keys (all must be mapped by ≥1 test): SPEC.md's unnumbered preamble; every numbered subsection; the section bodies of 3, 4, 5, 7, 8, 9, 10, 11, 14, 15. S-1 fails on any unmapped key, on a test mapping to a nonexistent key, and — conservative extension — on a map entry whose test ID has no implemented test or an implemented product-facing test missing from the map. Schedule after the SUITE/PROP tasks (S-1 goes green only when the suite is complete). Cites §0 H-7, §17 S-1. Verify: S-1 passes via `npm run test:self`.
- [ ] **CI-01 — E-6 Windows subset + Linux-side exchange writer.** Implement in `test/windows/`: T1.5-1, T1.5-3, T12.0-5 (less its Linux-only non-UTF-8 arm), and the single-casing probes of T10.1-2, T10.1-3, T12.0-6, T7-4; plus the representative byte-identity fixture (build, check, query, coverage, impact, journaled `rename`, journaled file-form `move` — the specifier-computation probe with post-move clean `check` — and an audit review session: `create --strategy audit`, `next --json`, a `resolve`, `export`), asserting reports, move-rewritten sources, generated files, emitted Markdown, graph data, journal, and session file byte-identical to the Linux leg's outputs read from `XSPEC_E6_EXCHANGE_DIR`. Implement the Linux-side writer (runs in the suite project, writes the same fixture's outputs into `XSPEC_E6_EXCHANGE_DIR` when set — see `.github/workflows/ci.yml` artifact plumbing). Git fixture scripted with pinned platform-independent author/committer/timestamps (HARNESS-01). The comparison fails loudly when the exchange dir is missing while the product exists; all these tests are product-facing (red against the stub is expected, as diagnosed failures — no crash on either leg). Cites §18 E-6, H-8. Verify: `npm run test:windows` collects the subset and fails-as-diagnosed against the stub.
- [ ] **CLEAN-01 — Scaffold removal + phase-gate confirmation.** Remove the three scaffold smoke tests (`test/self/scaffold.smoke.test.ts`, `test/suite/scaffold.smoke.test.ts`, `test/windows/scaffold.smoke.test.ts` — each self-labeled for removal) once their projects hold real tests. Confirm the phase gate: `npm run test:self` green twice consecutively with identical results (E-5, fixed seeds); the self project passes under parallel execution (E-3, H-1); `harness-self` CI job green with certification results in its output (C-1); `suite-linux` and `suite-windows` red is the expected pre-product baseline (product-facing tests fail as diagnosed assertions only — audit for any crash/hang/false-pass and fix in the harness). E-1's full-suite content (sections 1–17 incl. certification) exists and runs network-disabled via the existing scripts. Cites §17, §18 E-1/E-3/E-5, H-8/H-9. Verify: as stated; then this file's last task is removed and the plan file deleted per PROCESS.md Ralph rules.
